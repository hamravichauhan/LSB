# LumiRights Website (Vite + React + Tailwind)

## Use
1) Install Node.js (v18+ recommended)
2) In this folder:

```bash
npm install
npm run dev
```

Then open the local URL shown in your terminal.

## What’s included
- Beautiful activist landing page (mission, work, events)
- Registration form (demo: saved in localStorage)
- News section (demo posts + search)

## To make it real (recommended)
- Connect registration to a backend (Supabase/Firebase/your API)
- Add email verification + captcha/rate limiting
- Add a real news feed (RSS/JSON) and moderation for submissions
- Add privacy policy + terms
